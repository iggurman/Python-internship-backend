"""Q4. Explain the purpose of the StopIteration exception. When and by whom is it raised?

    A) StopIterator is used when the iterator has no more values to return so it signals the end of an iteration 
    B) its raised by __next__ dunder method is called when no more iterators are left to return values 
    C) iterator itself raises the StopIteration method using __next__ dunder method

"""
